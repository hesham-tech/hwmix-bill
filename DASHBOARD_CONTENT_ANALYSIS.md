# 📊 التحليل الشامل والكتالوج المرجعي لمحتويات لوحة التحكم (DASHBOARD_CONTENT_ANALYSIS)
## مشروع HWNix ERP — نظام إداري ومالي متعدد الشركات والمستأجرين (SaaS)

---

## 📌 1. ملخص تنفيذي (Executive Summary)

يمثل هذا التحليل المرجع الهندسي والتشغيلي الرسمي لإعادة بناء وتصميم **محرك لوحة التحكم (Dashboard Engine)** في مشروع **HWNix ERP**. تم صياغة هذه الوثيقة بناءً على **الغوص الفعلي داخل الكود المصدري، وبنية قاعدة البيانات، وعلاقات الكيانات، والمنطق المالي والمخزني المعتمد في المشروع**.

الهدف الأساسي هو تحويل لوحة التحكم من شاشة ساكنة إلى **بيئة عمل تفاعلية ذكية (Actionable Workspace)** تساعد كل مستخدم على بدء مهامه اليومية ومراقبة مؤشراته الحساسة فور تسجيل دخوله. يتم عزل كافة البيانات بناءً على هوية الشركة النشطة (`company_id`) والفرع النشط (`branch_id`) لضمان الامتثال التام لمعايير الـ Multi-Tenant SaaS وصلاحيات نظام Spatie Teams المطبق في النظام.

---

## 🧠 2. فلسفة لوحة التحكم في HWNix (Dashboard Philosophy)

لوحة التحكم في HWNix ERP ليست مجرد صفحة تقارير إحصائية ساكنة أو لوحة لعرض الرسوم البيانية والأرقام التراكمية. بل هي تمثل **نواة التشغيل والقيادة اليومية للمستخدم**، وتتمحور حول فلسفتين أساسيتين:

```
┌───────────────────────────────────────────────────────────────────┐
│                    HWNix Dashboard Philosophy                     │
├───────────────────────────────────┬───────────────────────────────┤
│    Decision Making Workspace      │      Actionable Workspace     │
├───────────────────────────────────┼───────────────────────────────┤
│ - معرفة الحالات الحرجة فور الدخول │ - استكمال العمل من حيث توقف    │
│ - اتخاذ القرار والاعتمادات السريعة│ - الوصول الفوري للروابط المفضلة│
│ - مراقبة كفاءة التحصيل والسيولة   │ - تنفيذ العمليات بضغطة واحدة   │
└───────────────────────────────────┴───────────────────────────────┘
```

### 2.1 بيئة اتخاذ القرار (Decision Making Workspace)
تستهدف تمكين متخذي القرار (ملاك، مدراء فروع، محاسبين) من تكوين رؤية واضحة ودقيقة عن الموقف التشغيلي والمالي للشركة فور فتح النظام:
* **تركيز الانتباه:** تسليط الضوء على الحالات الحرجة والمشاكل التي تتطلب تدخلاً فورياً (مثل عجز تسويات الخزن، نفاد مخزون أصناف رئيسية، أو تجاوز العملاء للحد الائتماني).
* **دعم الاعتمادات:** توفير طابور اعتمادات تفاعلي يتيح للمشرفين الموافقة على المستندات والتحويلات النقدية وتسويات الصناديق فورياً.

### 2.2 بيئة التشغيل الفعال (Actionable Workspace)
تستهدف تيسير وإنجاز العمل اليومي لموظفي المبيعات والتحصيل وأمناء الخزن دون التنقل المعقد في القوائم:
* **استكمال العمل:** إتاحة استئناف العمل على آخر السجلات والمسودات المفتوحة (Continue Working).
* **إنجاز المعاملات بضغطة واحدة:** توفير أزرار الإجراءات السريعة (Quick Actions) لتسجيل المقبوضات وصرف المصاريف وتحصيل الأقساط مباشرة.
* **الإنتاجية الفردية:** ربط لوحة التحكم بمهام المستخدم المفتوحة وجدول تحصيلاته ومفضلاته الشخصية لتصبح لوحة التحكم بمثابة مكتبه الرقمي اليومي.

---

## 🏗️ 3. النموذج المعماري العام للـ Widgets (Widget Architectural Blueprint)

لتجنب البناء العشوائي للأدوات، يلتزم النظام بنموذج معماري موحد وهيكلية برمجية ثابتة تُطبق على جميع الـ Widgets داخل **Dashboard Engine**. يوضح الجدول التالي الخصائص المعمارية المعتمدة لكل Widget:

| الخاصية المعمارية | الوصف والتطبيق الفعلي |
| :--- | :--- |
| **دورة الحياة (Lifecycle)** | تخضع لـ 4 مراحل برمجية: التأسيس (`initialize`)، جلب البيانات (`fetch`)، التحديث الدوري/الحدثي (`update/refresh`)، والتدمير عند الإغلاق (`destroy`) لتحرير الذاكرة. |
| **البيانات الوصفية (Metadata)** | يمتلك كل Widget سجل تعريف يتضمن: المعرف الفريد (`widget_id`)، الاسم، الوصف، الفئة، والمؤلف لسهولة الإدارة. |
| **الحالات البرمجية (States)** | يمر المكون بـ 6 حالات بصرية محددة: التأسيس (`Init`)، التحميل (`Loading`)، الجاهزية (`Ready`)، الخطأ (`Error`)، الفراغ (`Empty`)، وعدم الاتصال (`Offline`). |
| **حالة التحميل (Loading State)** | استخدام هياكل العظام البرمجية (**Skeleton Loaders**) المطابقة لأبعاد الـ Widget لمنع حدوث وميض أو اهتزاز في الصفحة أثناء جلب البيانات. |
| **حالة الفراغ (Empty State)** | عند خلو البيانات، يعرض المكون رسالة توضيحية خفيفة وأيقونة تعبر عن الفئة مع زر إجراء سريع (مثال: "لا توجد مهام اليوم، انقر هنا لإنشاء مهمة"). |
| **حالة الخطأ (Error State)** | معالجة استثناءات الاتصال والـ API بأمان وعرض خطأ مبسط للمستخدم مع توفير زر "إعادة المحاولة" (`Retry`)، وإرسال سجل الخطأ لـ `ErrorReportService`. |
| **سلوك وضع عدم الاتصال (Offline)** | عرض آخر البيانات التي تم جلبها وستحفظ محلياً في الـ Local Storage أو IndexedDB وتلوين شريط الحالة بلون رمادي للإشارة لعدم الاتصال. |
| **التحديث الدوري واللحظي** | يدعم المكون 3 استراتيجيات للتحديث: يدوي بالكامل، دوري بالطلب المتكرر (`Polling`) بفواصل زمنية، أو لحظي عبر أحداث Reverb Websockets للـ Widgets الحرجة. |
| **استراتيجية الكاش (Caching)** | تحديد مدة صلاحية بيانات الـ Widget بالسيرفر (مثال: كاش لمدة 60 دقيقة للرسوم البيانية الكبيرة) لتجنب الضغط على السيرفر في الاستضافات المشتركة. |
| **الاعتماديات والروابط** | توثيق اعتمادية المكون على موديولات معينة أو صلاحيات Spatie المحددة، أو كونه يعتمد على تغيير قيمة ويدجت آخر (مثل فلتر فروع مشترك). |
| **الأولوية (Priority)** | رتبة رقمية (1-3) تحدد ترتيب تحميل المكون في الصفحة (أولوية 1 تحمل فوراً، أولوية 3 تؤجل حتى اكتمال الصفحة). |
| **الأبعاد والتحجيم (Grid Size)** | تحديد أبعاد العرض المثالية والحد الأدنى والأقصى على نظام تخطيط شبكي ذكي (Grid Layout) مثل: 1x1 للـ Mini Cards، 2x2 للـ Charts، و 4x2 للجداول الكبيرة. |

---

## 📡 4. مصادر البيانات وإلغاء الاعتمادية على الجداول (Service-Layer Architecture)

التزاماً بدستور المعمارية المالية [FAC-001](file:///d:/Dev/projects/hwnix-bill/FAC-001.md)، **يُمنع منعاً باتاً استعلام الـ Widgets مباشرة من قاعدة البيانات أو الاعتماد على الجداول وعلاقاتها الثنائية (Database Layer)**. بدلاً من ذلك، تعتمد لوحة التحكم بالكامل على **طبقة الخدمات التشغيلية (Service Layer / Business Layer)** لجلب وتدقيق الأرقام، مما يضمن اتساق البيانات وتطبيق منطق الحماية:

```
┌────────────────────────────────────────────────────────┐
│              لوحة التحكم (Dashboard View)              │
└───────────────────────────┬────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────┐
│             طبقة الخدمات (Business Layer)              │
│ - FinancialEngine         │ - StatisticsService        │
│ - InventoryService        │ - InstallmentService       │
│ - ReportingService        │ - TaskService              │
└───────────────────────────┬────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────┐
│            طبقة قاعدة البيانات (Database Layer)        │
│ - transactions            │ - stocks                   │
│ - installments            │ - invoices                 │
└────────────────────────────────────────────────────────┘
```

* **`FinancialEngine` (طبقة التوافقية والمالية):** مصدر الحسابات النقدية الدقيقة وأرصدة السيولة الجارية للخزن ومطابقة عجز وزيادة تسويات الصناديق.
* **`StatisticsService` (إحصاءات المبيعات والأداء):** الخدمة المسؤولة عن قراءة ملخصات المبيعات اليومية والشهرية التراكمية وسلوك تفاعل المستخدمين مع المنتجات.
* **`InventoryService` (المخزون والمستودعات):** حساب قيمة المخزون بالتكلفة التاريخية، وتحديد الأصناف التي وصلت لحد إعادة الطلب، وتتبع الحركات المخزنية بين الفروع.
* **`InstallmentService` (خدمة الأقساط والتحصيل):** جلب الأقساط المستحقة وتواريخها، واحتساب كفاءة التحصيل التراكمية، وجدولة خطط الأقساط.
* **`ReportingService` (التقارير المحاسبية):** حساب الأرباح والخسائر الصافية التاريخية، وهيكل المصروفات التشغيلية، والتدفقات النقدية المتوقعة بناءً على الدفاتر.
* **`TaskService` (خدمة المهام والإنتاجية):** تتبع المهام المفتوحة والمسندة وحالاتها ومرفقاتها.
* **`BackupService` (خدمة النسخ والأمان):** تتبع حالة النسخ الاحتياطية وإعدادات الأرشفة السحابية.

---

## 🗂️ 5. التصنيف المعماري لكتالوج الـ Widgets (Widgets Categorized Catalog)

تمت إعادة هيكلة كتالوج الـ Widgets وتصنيفه معمارياً وفق الموديولات الفعليّة والخدمات الحاكمة بالنظام:

### 5.1 الأدوات المالية (Financial Widgets — Code: `FIN-*`)

#### 🔹 `FIN-001` مؤشر السيولة النقدية الجارية (Cash Liquidity Card)
* **الهدف:** توفير لقطة سريعة ومحدثة لإجمالي الكاش المتوفر بالخزن.
* **الوصف:** بطاقة رقمية تعرض مجموع رصيد كافة خزن الشركة المتاحة للمستخدم بناءً على فلاتر الصلاحية.
* **مصدر البيانات (Service):** `FinancialEngine::getCashBoxesBalanceSummary()`.
* **الكيانات المعتمد عليها:** `CashBox`.
* **لمن تظهر:** صاحب الشركة، المحاسب، مدير الشركة.
* **الأهمية:** حرج جداً (Critical).
* **الحالة والكاش:** Live (تحدث فوريًا مع حركات القبض والصرف).
* **الأبعاد المقترحة:** `Recommended: 1x1` | `Min: 1x1` | `Max: 2x1`.

#### 🔹 `FIN-002` بطاقة الأرباح والخسائر الصافية (Net Profit Card)
* **الهدف:** مراقبة صافي الأرباح المحققة خلال الفترة المحددة.
* **الوصف:** تعرض صافي الربح الفعلي بعد خصم COGS والمصروفات التشغيلية من إجمالي الإيرادات.
* **مصدر البيانات (Service):** `ReportingService::getNetProfitLossSummary($period)`.
* **الكيانات المعتمد عليها:** `DailySalesSummary`, `Invoice`, `Expense`, `Revenue`.
* **لمن تظهر:** صاحب الشركة، المحاسب المالي.
* **الأهمية:** حرج (Critical).
* **الحالة والكاش:** Cached (تخزين مؤقت لمدة 60 دقيقة لتقليل الحمل).
* **الأبعاد المقترحة:** `Recommended: 1x1` | `Min: 1x1` | `Max: 2x1`.

#### 🔹 `FIN-003` هيكل المصروفات التشغيلية (Expenses Distribution Wheel)
* **الهدف:** تحديد بنود الصرف الأكثر استهلاكاً للكاش.
* **الوصف:** رسم بياني دائري (Donut Chart) يوضح النسب المئوية للمصروفات حسب فئاتها التأسيسية.
* **مصدر البيانات (Service):** `ReportingService::getExpensesByCategoryDistribution($period)`.
* **الكيانات المعتمد عليها:** `Expense`, `ExpenseCategory`.
* **لمن تظهر:** صاحب الشركة، المحاسب.
* **الأهمية:** متوسطة (Medium).
* **الحالة والكاش:** Cached (تخزين مؤقت لمدة 120 دقيقة).
* **الأبعاد المقترحة:** `Recommended: 2x2` | `Min: 2x2` | `Max: 3x2`.

---

### 5.2 الأدوات التشغيلية (Operational Widgets — Code: `OPR-*`)

#### 🔹 `OPR-001` تنبيهات حد إعادة الطلب للمخزون (Reorder Points Alerts)
* **الهدف:** منع نفاد الكميات وتنبيه المشتريات للتوريد الفوري.
* **الوصف:** بطاقة تنبيهية مرنة تعرض عدد الأصناف التي انخفض رصيدها الفعلي عن `min_quantity` المحدد.
* **مصدر البيانات (Service):** `InventoryService::getReorderAlertsSummary()`.
* **الكيانات المعتمد عليها:** `Stock`, `ProductVariant`.
* **لمن تظهر:** مدير المخزن، موظف المشتريات، مدير الفرع.
* **الأهمية:** عالية (High).
* **الحالة والكاش:** Live.
* **الأبعاد المقترحة:** `Recommended: 2x1` | `Min: 1x1` | `Max: 2x2`.

#### 🔹 `OPR-002` قائمة المنتجات الأكثر حركة ومبيعاً (Top Selling Products)
* **الهدف:** تحديد المنتجات الأعلى طلباً وربحية بالفرع.
* **الوصف:** جدول مرتب تنازلياً للمنتجات الـ 5 الأكثر حركة بالكميات وصافي الربح المحقق.
* **مصدر البيانات (Service):** `StatisticsService::getTopSellingProducts($limit = 5)`.
* **الكيانات المعتمد عليها:** `StatsProductSummary`, `ProductVariant`.
* **لمن تظهر:** مدير الفرع، مدير المخزن، المالك.
* **الأهمية:** متوسطة (Medium).
* **الحالة والكاش:** Cached (تحديث يومي مسبق الصنع).
* **الأبعاد المقترحة:** `Recommended: 2x2` | `Min: 2x1` | `Max: 3x2`.

---

### 5.3 أدوات سير العمل والاعتمادات (Workflow Widgets — Code: `WFK-*`)

#### 🔹 `WFK-001` طابور اعتمادات الفواتير والعمليات (Approval Queue)
* **الهدف:** تسريع ترحيل المستندات المعلقة لمنع اختناق العمليات.
* **الوصف:** جدول يعرض أحدث فواتير البيع والشراء وتسويات الصناديق بحالة `Pending Approval` مع زر اعتماد فوري.
* **مصدر البيانات (Service):** `DashboardService::getPendingApprovalsQueue()`.
* **الكيانات المعتمد عليها:** `Invoice`, `CashReconciliation`, `FinancialOperation`.
* **لمن تظهر:** مدير الشركة، المشرف العام.
* **الأهمية:** حرج (Critical).
* **الحالة والكاش:** Live (تحديث فوري لحظي).
* **الأبعاد المقترحة:** `Recommended: 4x2` | `Min: 3x2` | `Max: 4x3`.

#### 🔹 `WFK-002` الأقساط المستحقة للتحصيل اليوم (Today's Collections pipeline)
* **الهدف:** توجيه محصلي الأقساط للعملاء المطلوب سدادهم اليوم.
* **الوصف:** قائمة تفاعلية بالأقساط التي يحل موعد استحقاقها اليوم مع خيار السداد السريع.
* **مصدر البيانات (Service):** `InstallmentService::getTodayDueInstallments()`.
* **الكيانات المعتمد عليها:** `Installment`, `User` (Customer).
* **لمن تظهر:** مندوب المبيعات، المحصل، أمين الخزينة.
* **الأهمية:** عالية (High).
* **الحالة والكاش:** Live.
* **الأبعاد المقترحة:** `Recommended: 3x2` | `Min: 2x2` | `Max: 4x2`.

---

### 5.4 أدوات الصحة الفنية والمراقبة (Monitoring Widgets — Code: `MON-*`)

#### 🔹 `MON-001` مؤشر السلامة المعمارية للحسابات (Financial Integrity Check)
* **الهدف:** كشف أي فروقات في الأرصدة أو عمليات تلاعب بالقيود التاريخية.
* **الوصف:** مؤشر مرئي يعرض حالة آخر عملية تدقيق مالي تلقائي وقيم الفروقات المكتشفة بالقرش.
* **مصدر البيانات (Service):** `FinancialEngine::getLatestIntegrityAuditReport()`.
* **الكيانات المعتمد عليها:** `FinancialLedger`, `Transaction`, `CashBox`.
* **لمن تظهر:** صاحب الشركة، المحاسب، الـ Super Admin.
* **الأهمية:** عالية (High).
* **الحالة والكاش:** Cached (تحديث مرتين يومياً عبر Cron).
* **الأبعاد المقترحة:** `Recommended: 2x1` | `Min: 1x1` | `Max: 2x2`.

#### 🔹 `MON-002` مؤشر أخطاء النظام البرمجية (Open Bugs & Errors Monitor)
* **الهدف:** رقابة استقرار الأكواد والخدمات في بيئة الـ SaaS.
* **الوصف:** كرت يعرض عدد تقارير الأخطاء البرمجية المفتوحة غير المعالجة.
* **مصدر البيانات (Service):** `DashboardService::getOpenErrorsCount()`.
* **الكيانات المعتمد عليها:** `ErrorReport`.
* **لمن تظهر:** الـ Super Admin.
* **الأهمية:** متوسطة (Medium).
* **الحالة والكاش:** Live.
* **الأبعاد المقترحة:** `Recommended: 1x1` | `Min: 1x1` | `Max: 1x1`.

---

### 5.5 أدوات الإنتاجية والأدوات الشخصية (Productivity & Personal — Code: `PRD-*` / `PER-*`)

#### 🔹 `PRD-001` قائمة مهامي المعلقة اليوم (My Pending Tasks List)
* **الهدف:** تنظيم يوم العمل للموظف.
* **الوصف:** قائمة تفاعلية بالمهام المفتوحة المسندة للمستخدم الحالي مرتبة بالأولوية مع إمكانية نقر الإنجاز السريع.
* **مصدر البيانات (Service):** `TaskService::getMyPendingTasks()`.
* **الكيانات المعتمد عليها:** `Task`, `TaskAssignment`.
* **لمن تظهر:** تظهر لجميع الموظفين والمستخدمين.
* **الأهمية:** عالية (High).
* **الحالة والكاش:** Live.
* **الأبعاد المقترحة:** `Recommended: 2x2` | `Min: 2x1` | `Max: 2x3`.

#### 🔹 `PER-001` استئناف العمل السريع (Continue Working Widget)
* **الهدف:** تقليص وقت الوصول وتسهيل العودة للمهام قيد الإنجاز.
* **الوصف:** يعرض آخر 5 فواتير أو مستندات أو عروض أسعار قام المستخدم الحالي بفتحها أو تعديلها ولم يرحلها بعد.
* **مصدر البيانات (Service):** `DashboardService::getUserRecentWorkspaceHistory()`.
* **الكيانات المعتمد عليها:** `ActivityLog`, `Invoice`, `Quotation`.
* **لمن تظهر:** تظهر لجميع الموظفين.
* **الأهمية:** متوسطة (Medium).
* **الحالة والكاش:** Live.
* **الأبعاد المقترحة:** `Recommended: 2x2` | `Min: 2x1` | `Max: 3x2`.

---

## 🎨 6. تجربة استخدام لوحة التحكم (Dashboard Experience Layer)

تم تصميم لوحة التحكم لتتخطى فكرة عرض الأرقام، لتصبح **منصة تشغيل تفاعلية متكاملة** تضمن العناصر التالية لتجربة المستخدم:

```
┌────────────────────────────────────────────────────────┐
│                 HWNix Dashboard Layout                 │
├────────────────────────────────────────────────────────┤
│  [1] Action Center (تنبيهات حرجة واعتمادات معلقة)      │
├────────────────────────────────────────────────────────┤
│  [2] Continue Working (استئناف العمل والمسودات)        │
├────────────────────────────────────────────────────────┤
│  [3] My Workspace (المهام اليومية والأقساط المستحقة)   │
├────────────────────────────────────────────────────────┤
│  [4] Quick Actions (شريط الإجراءات السريعة الفورية)     │
└────────────────────────────────────────────────────────┘
```

### 6.1 استئناف العمل الفوري (Continue Working)
يعرض هذا القسم تلقائياً للمستخدم السجلات التي كان يتعامل معها قبل خروجه من النظام (مثل فاتورة مسودة `Draft` قيد المراجعة أو عرض سعر لم يرسل بعد)، مما يتيح له نقر السجل والعودة للشاشة السابقة فوراً دون الحاجة للبحث عنها في الموديول.

### 6.2 مركز الإجراءات السريعة (Quick Actions Center)
شريط علوي عائم يحتوي على أزرار إجراءات مخصصة بضغطة واحدة تعتمد على صلاحيات المستخدم، مثل (إنشاء فاتورة سريعة، تسجيل قسط، إيداع عهدة) مما يقلص خطوات إدخال البيانات المعتادة.

### 6.3 مركز الإجراء والردود (Action Center)
مساحة مخصصة للإشعارات التفاعلية والطلبات التي تنتظر رداً فورياً من المستخدم (مثل طلب تحويل كاش وارد من خزنة أخرى ينتظر تأكيد الاستلام، أو طلب تسوية جرد بانتظار الاعتماد).

### 6.4 التوصيات والخيارات الذكية (Smart Recommendations)
ويدجت مستقبلي مدعوم بالذكاء الاصطناعي يقوم بتحليل سلوك المستخدم التشغيلي واقتراح إجراءات ذكية (مثال: "قارب رصيد الصنف X على النفاد بفرعك، هل تود إنشاء طلب توريد؟" أو "العميل Y متأخر في سداد قسطه وتاريخ اتصاله اليوم، هل تود الاتصال به؟").

---

## 📦 7. حزم لوحات التحكم الجاهزة (Dashboard Packages)

يوفر محرك لوحات التحكم حزماً مُعدة مسبقاً (Packages) تناسب الأقسام والوظائف المختلفة لتسهيل التهيئة الأولية للشركة:

1. **لوحة تحكم المالك (Owner Dashboard Package):**
   * *التركيز:* الأرباح والخسائر، السيولة الجارية بجميع الفروع، والتدفقات المالية الإجمالية.
   * *الـ Widgets الحاكمة:* `FIN-001`، `FIN-002`، `FIN-003`، `MON-001`.
2. **لوحة تحكم المحاسب (Accountant Dashboard Package):**
   * *التركيز:* قيود اليومية، تسويات الخزن، إقفال الفترات، والضرائب والمصروفات.
   * *الـ Widgets الحاكمة:* `FIN-001`، `FIN-003`، `MON-001`، طابور تسويات الصناديق.
3. **لوحة تحكم المخازن (Warehouse Dashboard Package):**
   * *التركيز:* حركة الأصناف، النواقص، التوريدات، والتحويلات المخزنية.
   * *الـ Widgets الحاكمة:* `OPR-001`، `OPR-002`، وجدول حركات المخزن الأخيرة.
4. **لوحة تحكم المبيعات والتحصيل (Sales & Collections Package):**
   * *التركيز:* أهداف المبيعات، عروض الأسعار، الأقساط المستحقة، ومعدل كفاءة التحصيل (CEI).
   * *الـ Widgets الحاكمة:* `WFK-002`، ومخططات عروض الأسعار، وكروت الأقساط المتأخرة `W-103`.
5. **لوحة تحكم الإدارة الفنية (System Health Package):**
   * *التركيز:* أمان ومراقبة المنصة والنسخ الاحتياطي والأخطاء.
   * *الـ Widgets الحاكمة:* `MON-002`، مؤشر النسخ الاحتياطي، وحالة السيرفر والاشتراكات.

---

## 🛡️ 8. مصفوفة مصادر الحقيقة (Source of Truth Matrix)

لضمان دقة الأرقام والنزاهة المالية، يحدد الجدول التالي مصدر الحقيقة الوحيد لكل فئة من فئات البيانات، وحالة الـ Snapshot وكيفية التعامل معها:

| اسم البيانات | مصدر الحقيقة الأصلي (Source of Truth) | هل يوجد Snapshot؟ | الغرض من الـ Snapshot | الخدمة المسؤولة (Owner Service) | السماح بالقراءة المباشرة؟ | السماح بالتعديل المباشر؟ | الطريقة الصحيحة للوصول والتعديل |
| :--- | :--- | :--- | :--- | :--- | :---: | :---: | :--- |
| **رصيد الخزينة (Cash Balance)** | مجموع الحركات الفعليّة في جدول `transactions` المرتبطة بـ `cashbox_id`. | نعم، حقل `cash_boxes.balance`. | تسريع استعلامات التقارير والتحقق من الأرصدة فوريًا. | `FinancialEngine` / `CashService` | ⚠️ للتقارير فقط | ❌ حظر مطلق | القراءة للتطبيقات والداشبورد عبر `FinancialEngine::getCashBoxBalance()`. التعديل حصرياً بالدوال `receiveMoney` / `payMoney` عبر المحرك المالي. |
| **رصيد ذمم الأطراف (Stakeholder Balance)** | مجموع الحركات الموثقة بـ `transactions` لـ `user_id` والشركة. | نعم، حقل `stakeholder_financial_balances.balance`. | تسريع عرض مديونية العميل ومستحقات المورد. | `FinancialEngine` / `ReceivableService` / `PayableService` | ⚠️ للتقارير فقط | ❌ حظر مطلق | القراءة عبر الـ Accessor المعتمد بموديل `User` أو الخدمة. التعديل حصرياً بدوال `createReceivable`/`reduceReceivable`/`createPayable`/`reducePayable` بالمحرك. |
| **كميات المخزون (Stock Quantity)** | رصيد المنتجات الفعلي بجدول `stocks.quantity`. | لا (تعتبر هي الحقيقة للكمية الجارية). | ➖ | `InventoryService` | ✅ نعم | ❌ حظر يدوي | القراءة مباشرة من الموديل أو الخدمة. التعديل ممنوع بشكل يدوي، ويتم تلقائياً عبر ترحيل الفواتير `SaleInvoiceService` / `PurchaseInvoiceService` أو التعديل اليدوي `manualAdjustment`. |
| **الكميات المحجوزة (Reserved Quantity)** | مجموع كميات بنود الفواتير المفتوحة بحالة `Draft` أو `Pending Approval`. | نعم، حقل `stocks.reserved`. | منع بيع الصنف نفسه مرتين (إجراء حجز وقائي). | `InventoryService` / `SaleInvoiceService` | ✅ نعم | ❌ حظر مطلق | القراءة من حقل `reserved` بالموديل. التحديث يتم تلقائياً عند إنشاء مسودة فاتورة أو اعتمادها أو إلغائها عبر أحداث الفواتير. |
| **تكلفة المنتج (Product Cost)** | سعر التكلفة الفعلي المسجل بـ `stocks.cost` للدفعة. | لا (تعتبر هي الحقيقة لتكلفة الدفعة). | ➖ | `InventoryService` | ✅ نعم | ⚠️ للمخازن فقط | القراءة عبر الخدمة. التعديل يتم فقط عبر ترحيل فاتورة شراء `PurchaseInvoiceService` أو التعديل اليدوي للمخزون بصلاحية المراجعة. |
| **الإحصاءات اليومية (Daily Stats)** | حسابات مجاميع الفواتير والمصروفات التفصيلية لليوم. | نعم، جدول `daily_sales_summary`. | تسريع شاشات الـ KPIs والرسوم البيانية بالداشبورد الرئيسية. | `StatisticsService` / `GenerateDailySummaries` command | ✅ نعم | ❌ حظر مطلق | القراءة والتحميل عبر `StatisticsService`. التحديث يتم كلياً عبر Cron Job مخصص أو أمر Artisan مجمع. |
| **الإحصاءات الشهرية (Monthly Stats)** | حسابات مجاميع الإحصاءات اليومية. | نعم، جدول `monthly_sales_summary`. | تسريع الرسوم البيانية للتدفقات النقدية السنوية والشهرية. | `StatisticsService` | ✅ نعم | ❌ حظر مطلق | القراءة والتحميل عبر `StatisticsService`. |

---

## 🔑 9. مصفوفة ملكية البيانات وتفويض الصلاحيات (Data Ownership Matrix)

تحدد هذه المصفوفة بشكل صارم الطبقات والخدمات البرمجية المخولة بإجراء عمليات التعديل أو القراءة، والطبقات الممنوع عليها التعديل منعاً لكسر سلامة المعمارية:

```
┌────────────────────────────────────────────────────────────────────────┐
│                        Data Ownership Matrix                           │
├───────────────────┬───────────────────────────┬────────────────────────┤
│   نوع البيانات    │   الخدمة المالكة للتعديل  │   الطبقات المحظورة     │
├───────────────────┼───────────────────────────┼────────────────────────┤
│  رصيد الخزينة     │ FinancialEngine / CashServ │ Controllers, Observers │
│  ذمم الأطراف      │ FinancialEngine / Receiv. │ Controllers, Observers │
│  كميات المخزون    │ InventoryService          │ Controllers, Payments  │
│  الكميات المحجوزة  │ InventoryServ / SaleInvo. │ Controllers, Payments  │
│  القيود المحاسبية  │ FinancialEngine / Ledger  │ Services, Controllers  │
│  الإحصاءات اليومية│ StatisticsService         │ Controllers, Views     │
└───────────────────┴───────────────────────────┴────────────────────────┘
```

1. **السيولة وأرصدة الخزن (Cash Balances):**
   * *المالك الوحيد للتعديل:* `FinancialEngine` بالتعاون مع `CashService`.
   * *الطبقات المحظورة:* يمنع منعاً باتاً على الـ Controllers أو Observers أو الموديلات مباشرة تحديث أرصدة الخزن.
2. **مديونيات وذمم الأطراف (Stakeholder Balances):**
   * *المالك الوحيد للتعديل:* `FinancialEngine` بالتعاون مع `ReceivableService` و `PayableService`.
   * *الطبقات المحظورة:* يمنع منعاً باتاً على الـ Controllers أو Observers أو نماذج الفواتير التعديل المباشر على أرصدة ذمم العملاء والموردين.
3. **كميات المخزون ورصيد الأصناف (Inventory Quantities):**
   * *المالك الوحيد للتعديل:* `InventoryService` (عبر استدعاءات فواتير البيع/الشراء أو التسويات اليدوية).
   * *الطبقات المحظورة:* يمنع على الـ Controllers أو موديولات الدفع أو الخزن التعديل مباشرة على كميات المخازن.
4. **القيود المحاسبية ودفتر الأستاذ (Ledgers & Entries):**
   * *المالك الوحيد للتعديل:* `FinancialEngine` بالتعاون مع `FinancialLedgerService`.
   * *الطبقات المحظورة:* يُحظر تماماً على أي خدمة تشغيلية (مثل الفواتير أو الأقساط) أو الـ Controllers إنشاء أو تعديل قيود الأستاذ العام مباشرة.
5. **الملخصات الإحصائية التراكمية (Pre-compiled Stats):**
   * *المالك الوحيد للتعديل:* `StatisticsService` عبر أمر Artisan المجدول تلقائياً.
   * *الطبقات المحظورة:* يُحظر على واجهات العرض أو الـ Controllers محاولة التحديث اليدوي المباشر لقيم الجداول الملخصة.

---

## 🏁 10. التوصيات الفنية لبناء محرك الداشبورد (Technical Architecture Recommendations)

تعتبر هذه التوصيات هي الدستور البرمجي لبناء محرك لوحة التحكم في المرحلة المقبلة:

1. **عزل البيانات المطلق (Multi-Tenant Isolation):**
   * يجب أن تخضع كافة استعلامات الداشبورد (SQL Queries) للتصفية المباشرة بناءً على `active_company_id` الخاص بالمستخدم النشط.
   * عند الاستعلام عن الفروع، يتم الفلترة بناءً على `branch_id` للتأكد من عدم تداخل الصلاحيات.
2. **الامتثال التام للـ FAC-001:**
   * يُمنع منعاً باتاً حساب السيولة أو الذمم من خارج جداول المحرك المالي المولدين لـ Ledger و Transactions لضمان دقة الأرقام المالية ومطابقتها التامة للواقع.
3. **تحسين الأداء في الاستضافة المشتركة (Shared Hosting Performance):**
   * **الاعتماد الأساسي على جداول الإحصاءات الملخصة (`daily_sales_summary` و `monthly_sales_summary`)** لعرض كروت الـ KPIs والرسوم البيانية بدلاً من الاستعلام المباشر لمئات الآلاف من جداول الفواتير والمصروفات.
   * **تخزين نتائج الـ Widgets المعقدة في الكاش (Caching System):** استخدام الكاش لتخزين إحصائيات المخزون وتحديثه دورياً عبر الجدولة (Cron Jobs) أو عند إطلاق أحداث برمجية محددة بالنظام.
   * **التحميل الكسول للـ Widgets (Lazy Loading):** تحميل الهيكل الأساسي للداشبورد فوراً، ثم استدعاء بيانات كل Widget بشكل مستقل عبر الـ API لضمان عدم بطء استجابة الصفحة الرئيسية للمستخدم.
4. **تخصيص الواجهات (Dynamic Widget Layout):**
   * حفظ مصفوفة تفضيلات المستخدم للداشبورد (توزيع الـ Widgets وحجمها وموقعها) في جدول `user_table_preferences` أو جدول مخصص لتفضيلات الداشبورد، لتسهيل تخصيص الواجهة لكل مستخدم مستقبلاً دون الحاجة لتغيير الكود.

---

> [!IMPORTANT]
> **تم إعداد وتحديث هذه الوثيقة بناءً على دراسة تفصيلية وبنيوية لمشروع HWNix ERP وكوده المصدري وقاعدة بياناته. يرجى مراجعتها وتأكيدها لاعتمادها كأساس وحيد رسمي لمرحلة بناء وتصميم محرك لوحة التحكم (Dashboard Engine) من الصفر.**
