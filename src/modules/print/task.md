# قائمة مهام تحسين وحدة الطباعة (Print Module Tasks) 🚀

تستعرض هذه القائمة المهام المطلوبة لرفع كفاءة وموثوقية نظام الطباعة بناءً على مخرجات التقرير النهائي الموحد.

## 🔴 مهام حرجة (Critical - High Priority)
- [x] **تنظيف الكود المتقادم:**
  - حذف ملف `src/modules/print/composables/usePrinter.js`.
  - تحديث كافة الاستدعاءات في `PrintDriver.vue` لتعتمد كلياً على `usePrint.ts` أو نظام الأحداث الجديد.
- [x] **تطوير نظام معالجة الأخطاء:**
  - إضافة `try...catch` في `PrintService.ts` و `usePrint.ts`.
  - توفير تغذية راجعة للمستخدم (Notification) عند فشل تحميل القالب أو إلغاء الطباعة.
- [x] **توحيد إعدادات الشركة:**
  - التأكد من جلب الشعار والاسم ونوع الطباعة الافتراضي من `useUserStore` بشكل موحد في جميع القوالب.

## 🟡 مهام تطوير الأداء (Performance - Medium Priority)
- [x] **تحويل آلية انتظار الصور:**
  - استبدال الـ (Polling Loop) في `PrintService.ts` بـ `MutationObserver` لمراقبة تحميل الباركود بشكل حدثي.
- [x] **نظام التخزين المؤقت (Caching):**
  - تنفيذ `barcodeCache` (Map) في قوالب الملصقات لمنع إعادة توليد نفس الباركود عدة مرات في الطلبات الكبيرة.
- [x] **تحسين جودة الباركود:**
  - تعديل ملف `ProductStickerTemplate.vue` لتثبيت أبعاد الباركود داخل إعدادات `JsBarcode` لضمان حدة الخطوط (Crisp edges).

## 🟡 مهام تجربة المستخدم (UX - Medium Priority)
- [x] **إضافة وضع المعاينة (Print Preview):**
  - إنشاء مكون `PrintPreview.vue` أو تعديل `PrintDriver.vue` لعرض المستند في Dialog قبل إرساله للطابعة.
- [x] **إضافة خيار الإغلاق التلقائي:**
  - تفعيل خيار `autoClose` لإغلاق نافذة المعاينة تلقائياً بعد نجاح الطباعة.

## 🟢 مهام معمارية وتنظيمية (Architecture - Low Priority)
- [x] **فصل الأنماط (CSS Separation):**
  - نقل سلاسل CSS من `index.ts` و `invoiceStyles.ts` إلى ملفات `.css` حقيقية لتحسين المقروئية ودعم الـ (IDE).
- [x] **مركزيّة الإعدادات (Configuration):**
  - نقل ثوابت أزمنة الانتظار (Timeouts) وروابط الخطوط إلى ملف `printConfig.ts`.
- [x] **التدويل (i18n):**
  - استبدال النصوص الثابتة (مثل "شكراً لتعاملكم معنا") بمفاتيح ترجمة قابلة للتغيير في `printConfig.ts`.

---
**حالة الوحدة:** `جاهزة للاستخدام` ✅  
*آخر تحديث: فبراير 2026*
