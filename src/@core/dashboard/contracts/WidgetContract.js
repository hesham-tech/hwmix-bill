/**
 * الكلاس الأساسي والتعريف المعماري لعقد المكون المالي والتشغيلي باللوحة لضمان موثوقية دورة الحياة والأداء
 */
export class BaseWidget {
  constructor() {
    this.widgetId = '';
    this.context = null;
  }

  /**
   * دالة التهيئة الأولية وحقن السياق
   * @param {object} context سياق عمل المكون المالي والتشغيلي
   */
  initialize(context) {
    this.context = context;
  }

  /**
   * استدعاء جلب البيانات من طبقة الخدمات
   * @returns {Promise<any>}
   */
  async fetchData() {
    throw new Error('يجب كتابة دالة fetchData داخل المكون الممتد.');
  }

  /**
   * التعامل مع أحداث تغيير حجم الشبكة البصرية
   * @param {object} newSize الأبعاد الجديدة للشبكة
   */
  onResize(newSize) {
    // اختياري للمكونات لإعادة تهيئة الرسوم البيانية
  }

  /**
   * استدعاء المكون عند تجميده في الخلفية لتوفير الأداء والشبكة
   */
  onSuspend() {
    // اختياري للمكونات لإيقاف الـ Polling
  }

  /**
   * تدمير المكون وإلغاء الاشتراك لتفادي تسريبات الذاكرة
   */
  onDestroy() {
    // تنظيف الأحداث والاشتراكات
  }
}
